CREATE TABLE IF NOT EXISTS vending_stock (
    id INT AUTO_INCREMENT PRIMARY KEY,
    machine_id VARCHAR(50) NOT NULL,
    item_name VARCHAR(50) NOT NULL,
    stock INT DEFAULT 0,
    UNIQUE(machine_id, item_name)
);
