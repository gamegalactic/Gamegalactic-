# qb-fps-cleaner

A lightweight FiveM client-side script for QBCore that removes excess greenery (trees, bushes, grass) to improve FPS — especially useful on Cayo Perico and other dense map areas.

----- Gamegalactic-----

## 🚀 Features

- Removes vegetation models to reduce client memory usage and improve performance
- Whitelist zones to preserve greenery in key areas (e.g. parks, cartel HQ)
- Command `/cleanfps` to toggle cleaner per player
- Auto-runs on Cayo Perico if enabled in config
- Fully client-side — no server sync impact

---

## 📦 Installation

1. Drop the `qb-fps-cleaner` folder into your server's `resources` directory.
2. Add this line to your `server.cfg`:
